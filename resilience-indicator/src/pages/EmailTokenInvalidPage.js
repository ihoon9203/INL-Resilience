import React from 'react';

const EmailTokenInvalidPage = function EmailTokenInvalidPageFunc() {
  return (
    <div style={{
      height: '100%',
      width: '100%',
      display: 'flex',
      flexDirection: 'column',
      padding: 300,
      paddingBottom: 300,
      justifyContent: 'center',
      textAlign: 'center',
    }}
    >
      <h1>Email validation token is invalid</h1>
    </div>

  );
};

export default EmailTokenInvalidPage;
